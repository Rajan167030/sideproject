WINDOWS QUICK RUN:

1) Backend:
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py

2) Frontend (in new terminal):
cd frontend
npm install
npm start

Note: Before running, replace backend/sample_data.json with the Reddit JSON you have (overwrite file).
